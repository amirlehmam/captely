FROM node:18-alpine

WORKDIR /app

# copy only package manifests, install deps
COPY package*.json ./
RUN npm install

# copy all source
COPY . .

EXPOSE 3000

# bind to 0.0.0.0 so Docker can route host traffic
CMD ["npm", "run", "dev", "--", "-p", "3000", "-H", "0.0.0.0"]
