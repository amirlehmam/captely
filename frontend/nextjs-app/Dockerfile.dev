FROM node:18-alpine

WORKDIR /app

# copy only package manifests, install deps
COPY package*.json ./
RUN npm install

# copy all source
COPY . .

EXPOSE 3000

# Use the long flags here instead of -p / -H
CMD ["npm","run","dev","--","--port","3000","--host","0.0.0.0"]